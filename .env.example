# Exemplo de variáveis de ambiente. Copie para .env e ajuste conforme o ambiente.
# Nunca commitar o .env real — segredos ficam nos GitHub Actions Secrets (produção)
# ou em um .env local ignorado pelo git (desenvolvimento).

DATABASE_URL=postgres://tarefazen:tarefazen@localhost:5432/tarefazen
PORT=3000
VITE_API_URL=http://localhost:3000
